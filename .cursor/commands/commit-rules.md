{
  "name": "Purple Group - Commit Messages and Branch Naming SOP",
  "command": "commit-rules",
  "prompt": "# Purple Group - Commit Messages and Branch Naming SOP\n\n## Always Ask for JIRA Board Name\nWhenever generating or validating commit messages, branch names, or anything that includes a JIRA ticket reference, always ask the user to provide the JIRA board name (e.g., EBA, TRD, SUP) unless it has already been specified.\n\n---\n\n## Commit Messages\nFollow the conventional commits standard with JIRA ticket numbers:\n\n**Pattern:** `<type>([optional scope]): <jira_ticket/s> <description>`\n\n**Allowed Types:**\n- `feature:` - New features (increments y in x.y.z)\n- `hotfix:` - Critical fixes\n- `fix:` - Bug fixes (increments z in x.y.z)\n- `build:` - Build system changes\n- `chore:` - Maintenance tasks\n- `ci:` - CI/CD changes\n- `docs:` - Documentation updates\n- `style:` - Code style changes\n- `refactor:` - Code refactoring\n- `perf:` - Performance improvements\n- `test:` - Test additions/changes\n- `update:` - General updates\n\n**Examples:**\n- Breaking change: `feature(api)!: EBA-1130 send an email to the customer when a product is shipped`\n- Feature: `feature: EBA-1147 allow provided config object to extend other configs`\n- Fix: `fix: EBA-1130 prevent racing of requests`\n- Multiple tickets: `fix: EBA-1130 EBA-1147 prevent racing of requests`\n\n**Notes:**\n- Use `!` for breaking changes (increments x in x.y.z)\n- Only the last commit in a PR is validated\n- Each PR update revalidates the latest commit\n\n---\n\n## Branch Names\n**Pattern:** `<type>/<jira-ticket>-<short-title>`\n\n**Examples:**\n- `feature/EBA-1130-mysql-connection-endpoints`\n- `fix/EBA-1147-kafka-streams-robustness`\n- `hotfix/EBA-1130-critical-db-fix`\n\n**Types for branches:**\n- `feature/` - New features\n- `fix/` - Bug fixes\n- `hotfix/` - Critical fixes\n- `docs/` - Documentation\n- `refactor/` - Code refactoring\n- `test/` - Testing\n- `chore/` - Maintenance\n\n---\n\n## Semantic Versioning\n- Breaking changes (`!`) increment x in x.y.z\n- Features increment y in x.y.z (overridden by breaking change)\n- Fixes increment z in x.y.z (overridden by features and breaking change)\n\n---\n\n## Final Notes\n- Always make sure that commit messages and branch names include the ticket number in all caps (EBA-1103 and not eba-1103)\n- Always include JIRA ticket numbers in both commit messages and branch names for proper changelog generation and JIRA integration.\n- Always ask for the JIRA board name if not explicitly given."
}
